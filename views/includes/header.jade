.container
	.row
		.page-header
			h1=title
			small 小标题测试
.navbar.navbar-default.navbar-fixed-bottom
	.container
		.navbar-header
			a.navbar-brand(href="/") 链接名称
		p.navbar-text.navbar-right
			a.navbar-link(href="#",data-toggle="modal",data-target="#singupModal") 注册
			span &nbsp;|&nbsp;
			a.navbar-link(href="#",data-toggle="modal",data-target="#singinModal") 登录
#singupModal.modal.fade
	.modal-dialog
		.modal-content
			form(method="POST",action="/user/singup")
			.modal-header 注册
			.modal-body
				.form-group
					label(for="singupName") 用户名
					input#singupName.form-control(name="user[name]",type="text")
				.form-group
					label(for="singunPassword") 密码
					input#singupPassword.form-control(name="user[password]",type="text")
			.modal-footer
				botton.btn.btn-default(type="button",data-dismiss="modal") 关闭
				botton.btn.btn-success(type="submit") 提交
#singinModal.modal.fade
	.modal-dialog
		.modal-content
			form(method="POST",action="/user/singin")
			.modal-header 登录
			.modal-body
				.form-group
					label(for="singinName") 用户名
					input#singinName.form-control(name="user[name]",type="text")
				.form-group
					label(for="singinPassword") 密码
					input#singinPassword.form-control(name="user[password]",type="text")
			.modal-footer
				botton.btn.btn-default(type="button",data-dismiss="modal") 关闭
				botton.btn.btn-success(type="submit") 登录